{
	"version": 2,
	"builds": [
		{
			"src": "api/index.js",
			"use": "@vercel/node"
		}
	],
	"rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "index.js"
		}
	],
	"github": {
		"silent": true
	}
}
